rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // dossier user images: /users/{uid}/...
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // autres fichiers blobs: restreindre
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
